<nav class="container mx-auto py-4 px-4" role="navigation" aria-label="Main">
  {% set menu = craft.entries().section("menu").all() %}
  <ul class="flex">

    {% if menu|length %}
    
      {% for menuItem in menu %}
        <li class="mr-6">
          <a class="text-blue-600 border-b border-blue-400' }}" href="{{ menuItem.urlLink }}"> {{ menuItem.title }} </a>
        </li>
      {% endfor %}

    {% endif %}

  </ul>
</nav>